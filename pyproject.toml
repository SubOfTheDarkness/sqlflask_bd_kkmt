[project]
name = "flaskr"
version = "1.0.0"
description = "Базовое приложение для блога, созданное по учебнику Flask."
dependencies = [
    "flask",
]

[build-system]
requires = ["flit_core<4"]
build-backend = "flit_core.buildapi"